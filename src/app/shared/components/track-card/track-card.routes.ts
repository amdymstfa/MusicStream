import { Routes } from "@angular/router";
import { TrackCardComponent } from "./track-card.component";

export const TRACK_CARD_ROUTES: Routes = [
    {
        path: '',
        component: TrackCardComponent
    }
];